import React from 'react';

const HeaderBar: React.FC = () => {
  return (
    <header className="fixed top-0 left-0 w-full bg-green-800 h-16 shadow-md">
      {/* A barra está vazia, mas você pode adicionar conteúdo aqui no futuro, se necessário */}
    </header>
  );
};

export default HeaderBar;
